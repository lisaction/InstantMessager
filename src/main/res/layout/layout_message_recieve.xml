<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimaryLight"
    android:paddingRight="80dip"
    android:paddingLeft="8dip"
    android:paddingBottom="10dip"
    android:paddingTop="10dip">

    <ImageView
        android:id="@+id/chat_image_other"
        android:layout_width="40dip"
        android:layout_height="40dip"
        android:layout_marginRight="5dip"
        android:layout_alignParentLeft="true"
        android:background="@drawable/icon_account"/>
    <TextView
        android:id="@+id/chat_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:theme="@style/chat_receive_style"
        android:layout_toRightOf="@id/chat_image_other"
        android:layout_alignTop="@id/chat_image_other"
        android:background="@drawable/chat_bubble_receive"
        android:textColor="@color/colorPrimaryText"
        android:text="the message received"      />




</RelativeLayout>